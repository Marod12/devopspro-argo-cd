apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: webcolor-canary
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Marod12/web-page-deploy.git
    targetRevision: HEAD
    path: k8s/canary
  destination:
    server: https://kubernetes.default.svc
    namespace: webcolor
  syncPolicy:
    syncOptions:
    - CreateNamespace=true
    automated:
      selfHeal: true
      prune: true
